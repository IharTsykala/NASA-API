import React, { useState } from "react"

// type SearchProps = {
//   dispatch: any,
// }

const CardItem: React.FunctionComponent = () => {
  return <div></div>
}

// const mapStateToProps = (state: any) => ({
//   searchStringState: state.pagination.searchString,
// })

export default CardItem
